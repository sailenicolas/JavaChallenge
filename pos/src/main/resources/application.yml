server:
  port: 8090

api:
  config:
    rest:
      cached:
        url: http://${HOST_CACHE}:8088/ms-cached
        debug: true
spring:
  mvc:
    servlet:
      path: /ms-pos
  webflux:
    base-path: /ms-pos
logging:
  level:
    root: debug
