server:
  port: 8089

spring:
  data:
    jdbc:
      dialect: postgresql
  jpa:
    hibernate:
      ddl-auto: create

  mvc:
    servlet:
      path: /ms-data
  webflux:
    base-path: /ms-data
logging:
  level:
    sql: debug
    org.springframework: DEBUG
