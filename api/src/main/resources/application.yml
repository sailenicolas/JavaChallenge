server:
  port: 8087
  servlet:
    context-path: /ms-api

api:
  config:
    rest:
      pos:
        url: http://${HOST_POS}:8090/ms-pos
        debug: true
      data:
        url: http://${HOST_DATA}:8089/ms-data
        debug: true
logging:
  level:
    root: debug
spring:
  webflux:
    base-path: /ms-api